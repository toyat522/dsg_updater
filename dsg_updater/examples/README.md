# Example usage for DSG Updater

1. Run `cypher_update_robot_state.py` to create or update robot state

```
python cypher_update_robot_state.py --name hamilton --x 1.0 --y 1.0
```

2. Run `cypher_get_robot_state.py` to confirm the updated robot state in the Heracles DB

